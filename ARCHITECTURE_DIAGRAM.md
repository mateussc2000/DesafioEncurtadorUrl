# 🎨 Arquitetura Visual - Fluxo de Dados

## 📐 Diagrama de Arquitetura

```
┌────────────────────────────────────────────────────────────────┐
│                        HTTP REQUEST                             │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  POST /v1/urls                                                  │
│  GET  /api/urls/{shortCode}                                     │
│  GET  /stats/{shortCode}                                        │
│  DELETE /{shortCode}                                            │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│              ShortenUrlController                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ @PostMapping("/v1/urls")                                │  │
│  │ @GetMapping("/api/urls/{shortCode}")                    │  │
│  │ @GetMapping("/stats/{shortCode}")                       │  │
│  │ @DeleteMapping("/{shortCode}")                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│              ShortenUrlService (Interface)                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ shortenUrl(request)                                      │  │
│  │ getShortenedUrl(shortCode)                               │  │
│  │ redirectToOriginalUrl(shortCode)                         │  │
│  │ deleteShortenedUrl(shortCode)                            │  │
│  │ getStats(shortCode)                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│           ShortenUrlServiceImpl (Implementação)                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ @Autowired                                               │  │
│  │ private ShortenedUrlRepository repository;              │  │
│  │                                                          │  │
│  │ @Autowired                                               │  │
│  │ private ShortenUrlMapper mapper; ✨ NOVO               │  │
│  │                                                          │  │
│  │ public ShortenUrlResponse shortenUrl(...) {             │  │
│  │   // ... lógica ...                                      │  │
│  │   return mapper.toResponse(saved); ✨                  │  │
│  │ }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│           ShortenedUrlRepository (JPA)                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ extends CrudRepository<ShortenedUrl, String>             │  │
│  │                                                          │  │
│  │ - Busca do banco de dados                               │  │
│  │ - Entidade JPA: ShortenedUrl                            │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
                              ↓
                        ShortenedUrl Entity
                   (id, originalUrl, createdAt, 
                    expirationDate, clickCount)
                              ↓
┌────────────────────────────────────────────────────────────────┐
│              ShortenUrlMapper ✨ NOVO                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ @Component                                               │  │
│  │ public class ShortenUrlMapper {                          │  │
│  │                                                          │  │
│  │   @Value("${app.base-url:...}")                          │  │
│  │   private String baseUrl;                                │  │
│  │                                                          │  │
│  │   public ShortenUrlResponse toResponse(               │  │
│  │       ShortenedUrl entity) { ... }                       │  │
│  │                                                          │  │
│  │   public DetailsUrlResponse toDetailsResponse(       │  │
│  │       ShortenedUrl entity) { ... }                       │  │
│  │ }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
                    ↙                ↖
    ┌──────────────────────┐   ┌──────────────────────────┐
    │                      │   │                          │
    ↓                      ↓   ↓                          ↓
┌──────────────────┐  ┌──────────────────────────────┐
│ ShortenUrlResponse✨ │  │ DetailsUrlResponse ✨      │
├──────────────────┤  ├──────────────────────────────┤
│ id               │  │ id                           │
│ shortUrl         │  │ shortUrl                     │
│ originalUrl      │  │ originalUrl                  │
│ createdAt        │  │ createdAt                    │
│ expirationDate   │  │ expirationDate               │
│                  │  │ clickCount ← Novo Campo      │
└──────────────────┘  └──────────────────────────────┘
    ↓                      ↓
    └──────────┬───────────┘
               ↓
        Response JSON
               ↓
    ┌─────────────────────┐
    │  HTTP RESPONSE 200  │
    └─────────────────────┘
```

---

## 🎯 Fluxo de Encurtamento de URL

```
                    POST /v1/urls
                          ↓
                 ┌────────────────┐
                 │ Request Payload │
                 │ {              │
                 │   "originalUrl":│
                 │   "..."        │
                 │ }              │
                 └────────────────┘
                          ↓
            ┌─────────────────────────┐
            │ ShortenUrlController    │
            │ .shortenUrl(request)    │
            └─────────────────────────┘
                          ↓
            ┌─────────────────────────┐
            │ Validações              │
            │ - URL válida?           │
            │ - Alias único?          │
            │ - Data expiração ok?    │
            └─────────────────────────┘
                          ↓
            ┌─────────────────────────┐
            │ Gerar short code        │
            │ ou usar alias           │
            └─────────────────────────┘
                          ↓
            ┌─────────────────────────┐
            │ Salvar no banco         │
            │ (ShortenedUrl)          │
            └─────────────────────────┘
                          ↓
            ┌─────────────────────────┐
            │ ShortenUrlMapper        │
            │ .toResponse(entity) ✨  │
            └─────────────────────────┘
                          ↓
            ┌─────────────────────────┐
            │ ShortenUrlResponse      │
            │ {                       │
            │   "id": "abc123",      │
            │   "shortUrl": "...",   │
            │   "originalUrl": "..." │
            │   ...                   │
            │ }                       │
            └─────────────────────────┘
                          ↓
                HTTP 201 Created
```

---

## 🔄 Fluxo de Obtenção de Estatísticas

```
                 GET /stats/{shortCode}
                          ↓
         ┌──────────────────────────────┐
         │ ShortenUrlController         │
         │ .getStats(shortCode)         │
         └──────────────────────────────┘
                          ↓
         ┌──────────────────────────────┐
         │ ShortenUrlRepository         │
         │ .findByShortCode(shortCode)  │
         └──────────────────────────────┘
                          ↓
         ┌──────────────────────────────┐
         │ Retorna: Optional<ShortenedUrl>
         │ {                            │
         │   id, originalUrl,           │
         │   createdAt,                 │
         │   expirationDate,            │
         │   clickCount ← Importante    │
         │ }                            │
         └──────────────────────────────┘
                          ↓
         ┌──────────────────────────────┐
         │ ShortenUrlMapper             │
         │ .toDetailsResponse(entity) ✨ │
         │                              │
         │ Inclui clickCount!           │
         └──────────────────────────────┘
                          ↓
         ┌──────────────────────────────┐
         │ DetailsUrlResponse ✨        │
         │ {                            │
         │   "id": "abc123",           │
         │   "shortUrl": "...",        │
         │   "originalUrl": "...",     │
         │   "createdAt": "...",       │
         │   "expirationDate": "...",  │
         │   "clickCount": 42 ✨       │
         │ }                            │
         └──────────────────────────────┘
                          ↓
                  HTTP 200 OK
```

---

## 📊 Comparação: Estruturas de Dados

### Antes (Confuso)
```
ShortenUrlResponse (8 campos)
├── shortCode         ← ID genérico
├── customAlias       ← Não usado em response
├── originalUrl       ✅
├── shortUrl          ✅
├── createdAt         ✅
├── expirationDate    ✅
├── clickCount        ← Nem sempre há
└── lastAccessed      ← Nunca usado
```

### Depois (Claro)
```
ShortenUrlResponse (5 campos) ✨
├── id                ✅ (antes: shortCode)
├── shortUrl          ✅
├── originalUrl       ✅
├── createdAt         ✅
└── expirationDate    ✅

DetailsUrlResponse (6 campos) ✨ NOVO
├── id                ✅
├── shortUrl          ✅
├── originalUrl       ✅
├── createdAt         ✅
├── expirationDate    ✅
└── clickCount        ✅ (agora específico)
```

---

## 🔧 Componentes da Solução

```
┌─────────────────────────────────────────┐
│ ShortenUrlMapper                        │
│ ✨ NOVO - Component Spring              │
├─────────────────────────────────────────┤
│                                         │
│  Responsabilidades:                     │
│  • Converter ShortenedUrl → DTO         │
│  • Construir shortUrl com baseUrl       │
│  • Oferecer múltiplas conversões        │
│                                         │
│  @Component                             │
│  public class ShortenUrlMapper {        │
│    @Value("${app.base-url:...}")       │
│    private String baseUrl;              │
│                                         │
│    public ShortenUrlResponse           │
│      toResponse(ShortenedUrl) { ... }  │
│                                         │
│    public DetailsUrlResponse           │
│      toDetailsResponse(ShortenedUrl)    │
│      { ... }                            │
│  }                                      │
│                                         │
└─────────────────────────────────────────┘
        ↑                      ↑
        │                      │
        └──┬──────────┬────────┘
           │          │
         (1)        (2)
           │          │
    ┌──────▼──┐  ┌────▼──────────┐
    │ Response │  │ DetailsResponse│
    │  Simple  │  │  Com Stats     │
    └──────────┘  └────────────────┘
```

---

## 🎯 Padrão Mapper Implementado

```
Entity (ShortenedUrl)
        ↓
        │ Responsabilidade: DADOS
        │ - Vem do banco
        │ - Contém tudo
        │
        ↓
┌──────────────────────┐
│ ShortenUrlMapper     │
│ (Conversion Logic)   │
├──────────────────────┤
│ Responsabilidade:    │
│ - Transformar dados  │
│ - Construir URLs     │
│ - Selecionar campos  │
│ - Reutilizável       │
└──────────────────────┘
        ↓
        │ Duas saídas possíveis:
        │
    ┌───┴───┐
    │       │
    ↓       ↓
  DTO1    DTO2
 Simple   Details
  (5)      (6)
 campos   campos
```

---

## 📈 Estatísticas de Transformação

```
Entrada:
┌─────────────────────────────────────┐
│ ShortenedUrl Entity                 │
├─────────────────────────────────────┤
│ shortCode: "abc123"                 │
│ originalUrl: "https://exemplo.com"  │
│ createdAt: 2026-03-11T10:30:00      │
│ updatedAt: 2026-03-11T10:30:00      │
│ clickCount: 42                       │
│ lastAccessed: 2026-03-11T15:45:30   │
│ expirationDate: 2026-04-11T10:30:00 │
│ customAlias: null                   │
│ baseUrl: http://localhost:8080      │
└─────────────────────────────────────┘
           ↓
    ┌──────┴──────┐
    │   Mapper    │
    │  Transforma │
    └──────┬──────┘
           ↓
    ┌──────────────────┐  ┌────────────────────┐
    │Response Simple   │  │DetailsResponse     │
    ├──────────────────┤  ├────────────────────┤
    │id: "abc123"      │  │id: "abc123"        │
    │shortUrl: "http:..│  │shortUrl: "http:... │
    │originalUrl: "..." │  │originalUrl: "...   │
    │createdAt: ...    │  │createdAt: ...      │
    │expirationDate: ..│  │expirationDate: ... │
    │                  │  │clickCount: 42 ✨   │
    └──────────────────┘  └────────────────────┘
         5 campos              6 campos
```

---

## 🚀 Injeção de Dependência

```
┌──────────────────────────────────┐
│ ShortenUrlServiceImpl             │
├──────────────────────────────────┤
│                                  │
│ @Autowired                       │
│ private ShortenedUrlRepository   │
│         repository; ✅ Existia    │
│                                  │
│ @Autowired                       │
│ private ShortenUrlMapper         │
│         mapper; ✨ NOVO          │
│                                  │
│ public ShortenUrlResponse        │
│   shortenUrl(...) {              │
│   // ...                          │
│   return mapper.toResponse(      │
│       saved                       │
│   ); ✨                          │
│ }                                │
│                                  │
└──────────────────────────────────┘
         ↑                  ↑
         │                  │
         └────────┬─────────┘
                  │
            Spring Container
            Gerencia injeção
            automáticamente
```

---

**Diagrama completo da arquitetura refatorada!** 🎨


